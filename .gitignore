# Claude local settings / agent state
.claude/

# Python
__pycache__/
*.pyc
*.pyo

# OS / editor
.DS_Store
Thumbs.db

# runtime DXF repair sidecars (created next to converted files)
*.fixed.dxf

# build artifacts
*.tar
*.tar.gz

# ODA File Converter .deb is licence-restricted — never commit it (provided via
# a GitHub Release / local build context instead)
app/oda/*.deb
