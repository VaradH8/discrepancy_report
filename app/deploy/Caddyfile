discrepancy.inventivebizsol.co.in {
	encode zstd gzip

	# Drawings can be large; allow generous upload bodies.
	request_body {
		max_size 200MB
	}

	reverse_proxy app:8000

	log {
		output file /data/access.log
		format console
	}
}
