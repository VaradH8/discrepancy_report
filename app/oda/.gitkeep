# Drop the ODA File Converter .deb here for a local build, or let the CI
# workflow fetch it from a GitHub Release tagged `oda`. The .deb is
# licence-restricted and is git-ignored (see root .gitignore).
